'use strict';
var app = app || {};

(function(module) {
  const portfolios = {};
  portfolios.all = [];

  portfolios.requestPortfolioPages = function(callback) {
    $.ajax({
      url: '',
      method: '',
    })
  }
}
